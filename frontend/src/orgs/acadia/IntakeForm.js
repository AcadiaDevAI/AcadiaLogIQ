// Acadia intake = the shared/default Tier-1 intake form. Re-exported here so
// the org registry has a concrete, lazy-loadable Acadia module. Other orgs
// inherit from this by re-exporting it.
export { default } from "../../components/Tier1Copilot/Tier1IntakeForm";
