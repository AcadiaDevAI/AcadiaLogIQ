// Acadia workspace/journey = the shared/default Tier-1 Workspace (the
// ResolutionJourney stages 0-5). Re-exported so the registry can lazy-load it.
export { default } from "../../components/Tier1Copilot/Tier1Workspace";
